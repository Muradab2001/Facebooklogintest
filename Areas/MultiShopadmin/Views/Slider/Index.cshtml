@model List<Slider>
@{
    ViewData["Title"] = "Index";
}

<div class="main-panel">
    <div>
        <a class="btn btn-success" asp-area="MultiShopadmin" asp-controller="slider" asp-action="Create">Create new slider</a>
    </div>
    <div class="table-responsive pt-3">
        <table class="table table-dark">
            <thead>
                <tr>
                    <th>
                        Image
                    </th>
                    <th>
                        Title
                    </th>
                    <th>
                      Desc
                    </th>
                    <th>
                        Settings
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (Slider slider in Model)
                {
                    <tr>
                        <td>
                            <img style="width:150px !important; height:150px; object-fit:contain" src="~/assets/img/@slider.Image"  alt="">
                        </td>
                        <td>
                            @slider.Title
                        </td>
                         <td>
                            @slider.Desc
                        </td>
                        <td style=" height: 197px; padding: 4rem 0;" class="d-flex">
                                <div>
                                    <a asp-area="MultiShopadmin" asp-controller="slider" asp-action="Edit" asp-route-id="@slider.Id" class="btn btn-warning"><i class="mdi mdi-border-color"></i></a>
                                </div>
                                <div>
                                    <a class="btn btn-danger" asp-area="MultiShopadmin" asp-controller="slider" asp-action="Delete" asp-route-id="@slider.Id"><i class="mdi mdi-delete"></i></a>
                                </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
